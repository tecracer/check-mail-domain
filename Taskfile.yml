# https://taskfile.dev

version: "3"

vars:
  GREETING: Hello, World!

tasks:
  default:
    cmds:
      - task -l

  build:
    desc: Build binarx
    cmds:
      - go build -o checkdns main.go
    silent: true
